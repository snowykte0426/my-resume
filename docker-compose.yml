version: '3.8'

services:
  resume:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-resume:latest
    container_name: my-resume
    ports:
      - "8080:4173"
    restart: unless-stopped
    networks:
      - resume-network

networks:
  resume-network:
    driver: bridge